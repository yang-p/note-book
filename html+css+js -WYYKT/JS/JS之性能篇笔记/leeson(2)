条件体很多时，switch的执行速度比if else快。
判断一个区间的离散值，采用二分法。
字符串连接的临时字符串问题：
            形如 str += "a" + "b" 
            系统执行四个步骤：创建一个临时字符串；字符a加b后赋给临时字符串；临时字符串与str相连。
           避免产生临时字符串可提高执行效率。
           解决办法：离散表达式 str += "a"; str += "b";
                     使用一行代码实现 ==> str += str + "a" + "b";//str的位置不能变，否则失去优化。

