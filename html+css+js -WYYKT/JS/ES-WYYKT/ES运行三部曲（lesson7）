js运行的三个步骤:
        ①语法分析      对代码进行全篇扫描，查看语法是否有错.
        ②预编译        函数声明整体提升 、变量的声明提升
        ③解释执行      由于是解释型语言，所以是解释一行执行一行
——————————————————————————————
预编译： 预编译是函数刚要执行之前的一个步骤，预编译的作用是为函数提供执行顺序。
 预编译四部曲:
               ①创建AO对象(activation object) ==> AO  { 执行期上下文}
【实参值不是直接与arguments的形参映射，而是在AO对象中的arguments与形参映射】
               ②在函数体中先找形参和变量声明；初始属性值为undefined。  若形参和变量名相同，只写一个。
                找形参的原因是新参就相当于在函数体里面进行变量声明。
【2017年谷歌内核不允许函数体中的if或for语句中有变量声明，该函数名在预编译时属性值变成了undefined】
               ③将实参值和形参统一，将实参值赋给形参。
               ④在函数体里面找函数声明，属性值为函数体。
   把一个script标签当成一个function，那么他的执行期上下文对象就是GO对象(gloabal object)。

函数在预编译中生成的AO对象有以下默认属性
     AO {
      arguments : {},
      this : window,
      形参 : undefined,
      变量 : undefined,
  函数声明 : 函数体
     }





















