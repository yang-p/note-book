浏览器是基于es3.0 + es5.0新增的方法 使用的。
但是有些方法es3.0能用，es5.0不能用，es5.0能用的方法而es3.0不能用，当起冲突时默认是使用es3.0的方法。
es5.0的严格模式：当执行es3.0和es5.0的产生冲突部分，首先使用es5.0的方法。
如何开启严格模式：
    全局内开启：在script标签里面的最顶端添加一个字符串，"use strict";（该字符串上面允许有空白，但是不能出现js语句）
    局部内开启(推荐)：在一个函数体的最顶端添加"use strict",代表只有该函数具备es5.0严格模式
    严格模式不会对不兼容严格模式的浏览器产生影响。

es5.0严格模式不兼容的方法(即影响运行效率的方法不予许使用)：
 with(参数a){代码块...}
        将参数a作为 花括号里面的所有属性和方法的 作用域链的最顶端。
      作用：改变作用域及作用域链，应用场景：命名空间 ，缺点：由于改变作用域链系统会重新整理作用域链大大降低运行效率。
      参数a是一个引用值，它改变了代码块里面的AO，即代码块的直接AO就是参数a指向的那个AO，
      当代码块在参数a指向的那个AO里面没找到相应的属性或方法，那么就从包含with语句的函数中找，直到GO。
      with的例子：
                  with(document){write('haha');}

 arguments.calle、arguments.caller
 严格模式注意点:
          ①变量未经声明不能使用； PrefrenceError
          ②预编译过程中局部的this不再指向window，而是undefined，但是当用new执行函数时返回 该constructor的名加一对花括号————>函数名{}
          而函数名.call(参数)，this指向参数。参数如果是原始值就返回该值，而不再是进行es3.0的包装类。
          ③参数名(参数名重复在es3.0不报错，在es5.0中报语法解析错误)和属性名(属性名重复在es3.0和es5.0中目前都不报错)不允许重复。