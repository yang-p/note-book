判断原始值还是引用值
        if(target != null && typeof(target) == 'object')//在目标不能为空的前提下返回object才是引用值

判断对象的属性或方法是继承自原型链还是属于本身
         if(target.hasOwnPrototype(target[i]))
判断一个引用值是数组还是对象
       obj.
判断一个方法在该浏览器中是否支持，若不支持该方法执行时会报错
  if(obj.该方法的引用) //在控制台直接输入“obj.该方法的引用”，若不支持，则返回undefined，若支持则返回true。








————————————————————————————————————————————————————————————
利用字符串加的形式，使另一个其他类型的原始值变成字符串。





