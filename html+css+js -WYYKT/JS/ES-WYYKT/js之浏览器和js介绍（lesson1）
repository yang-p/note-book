浏览器的组成 : shell(浏览器的外壳)、内核(渲染引擎、js引擎)
主流浏览器及其内核:
             IE                tritent(IE9及以下，IE10及以上用webkit)
             Chrome            webkit/blink
             Firefox           Gecko
             opera             presto(旧版本 ，新版本使用webkit)
             safrai            webkit
————————————————————————————————————————————————————————————
JavaScript特点：
    解释性语言
           【拓展】编译性语言：通篇翻译一遍代码，翻译完成后生成一个文件，
                   然后才执行翻译完后的这个文件， 正是生成的文件使得该语言不能跨平台，Java不是编译性语言而是Oak语言，因为它有JVM。
    单线程：js是单线程，具体来说js引擎是单线程。
                  【拓展】脚本语言、动态语言
JavaScript的执行队列(轮转时间片)
                 js引擎将所有任务以微秒或毫秒拆分为很多个片段，然后将片段随机排成一个队列，
                 加快执行速度，这样就达到了同时执行多个任务感觉。
                 【拓展】轮转时间片也是操作系统的原理。
JavaScript的引入：
                内部js文件 <script>、</script> 外部js文件 <script src = "js文件路径"></script>
                一个script标签同时引入外部js文件和在标签里面写js代码时，只有引入的js文件能生效。
js的注释： 单行注释//、多行注释/**/



