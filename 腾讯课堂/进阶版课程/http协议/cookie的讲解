由于http协议是无状态的协议，所以使用cookie帮助服务器识别用户（客户端），实现持久会话的最好方式

cookie的类型：
            会话cookie
            持久cookie

cookie的工作原理：
            当客户端第一次访问服务器时，服务器会在响应首部设置一个该客户端特有的cookie
            客户端记住响应首部的cookie内容，当客户端再次访问同一个服务器时，客户端会将请求首部的cookie放松给服务器