全局过滤器：
			Vue.filter('过滤器名称'，function(value){})
			回调函数的第一个参数是，目标数据的值，第二个参数及其后面的参数是过滤器的实参。
			对目标数据的值改变后，需要将改变的值返回出去。

双向过滤器：
			Vue.filter('过滤器名称',{
				read:function(){

				},
				write:function(){
				
				}
			})