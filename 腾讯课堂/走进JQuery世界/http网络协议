五层网络协议：
			应用层：http协议、FTP（下载）、DNS、SMTP（邮件）、Telnet
			传输层：TCP/UDP协议
			网络层：IP地址、ICMP、RIP、BGP
			数据链路层：如何传输
			物理层：OSI的物理规范

七层网络协议：
			应用层：http协议
			表示层：数据加密，格式转换
			会话层：解除或者建立其他节点的联系
			传输层：TCP/UDP协议
			网络层：IP地址
			数据链路层：如何传输
			物理层：OSI的物理规范

200	 	目标服务器成功收到
404		没找到目标服务器
302		服务器地址重定向了(新的ip地址)
502		目标服务器不接收